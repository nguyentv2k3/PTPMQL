@model Baithuchanh.Models.BMIModel

<h2>Tính chỉ số BMI</h2>

<form asp-action="Index" method="post">
    <div>
        <label>Cân nặng (kg):</label>
        <input asp-for="CanNang" type="number" step="0.1" required />
    </div>
    <div>
        <label>Chiều cao (m):</label>
        <input asp-for="ChieuCao" type="number" step="0.01" required />
    </div>
    <button type="submit">Tính BMI</button>
</form>

@if (Model?.BMI != null)
{
    <p>Chỉ số BMI của bạn là: <strong>@Model.BMI.Value.ToString("0.00")</strong></p>
    <p>@ViewBag.ThongBao</p>
}
