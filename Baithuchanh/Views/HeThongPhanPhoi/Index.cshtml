@model IEnumerable<Baithuchanh.Models.HeThongPhanPhoi>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenHTPP)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenHTPP)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaHTTP">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaHTTP">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaHTTP">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
